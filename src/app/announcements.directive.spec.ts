import { AnnouncementsDirective } from './announcements.directive';

describe('AnnouncementsDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnouncementsDirective();
    expect(directive).toBeTruthy();
  });
});
