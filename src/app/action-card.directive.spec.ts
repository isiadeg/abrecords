import { ActionCardDirective } from './action-card.directive';

describe('ActionCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionCardDirective();
    expect(directive).toBeTruthy();
  });
});
